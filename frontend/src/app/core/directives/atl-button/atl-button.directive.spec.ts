import { AtlButtonDirective } from './atl-button.directive';

describe('AtlButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new AtlButtonDirective();
    expect(directive).toBeTruthy();
  });
});
